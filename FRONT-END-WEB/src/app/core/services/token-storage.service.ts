import { Injectable } from '@angular/core'; @Injectable({providedIn:'root'}) export class TokenStorageService{ private tokenKey='semapam_admin_token'; private profileKey='semapam_admin_profile'; setToken(token:string){localStorage.setItem(this.tokenKey,token)} getToken(){return localStorage.getItem(this.tokenKey)} clear(){localStorage.removeItem(this.tokenKey);localStorage.removeItem(this.profileKey)} setProfile(profile:unknown){localStorage.setItem(this.profileKey,JSON.stringify(profile))} getProfile<T>(){const raw=localStorage.getItem(this.profileKey); return raw?JSON.parse(raw) as T:null;} }
